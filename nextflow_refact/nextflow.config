process {
    
    cpus = Runtime.runtime.availableProcessors()
} 


params {
    length = 1
    genome_loc = '../test_files'
    search_db = '../test_files/virus_subset.fa'
    mode = "fast"
    rangejunction = 100
}